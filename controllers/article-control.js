var self = module.exports = {

	init: function (app, service, hbs) {

		// // render an article for ajax calls
		// app.get('/article/:id', function(req, res) {
		// 	var id = req.params.id;
		// 	var isId = id.match(/^\d+$/);

		// 	// TODO ... actually throw error? or should this reroute to 404 using middleware?
		// 	if(isId) {
		// 		console.log('CS: '.blue + 'Article accessed at id: '.green + id.yellow);
		// 		res.send('<p style="background-color:yellow;">This is what an article resource looks like...sort of...</p>');
		// 	} else {
		// 		res.send('error!');
		// 	}
		// });

	}

};


